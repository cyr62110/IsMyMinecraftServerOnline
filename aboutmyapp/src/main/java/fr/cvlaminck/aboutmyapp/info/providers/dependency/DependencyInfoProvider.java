package fr.cvlaminck.aboutmyapp.info.providers.dependency;

/**
 * An InfoProvider for DependencyEntity.
 * <p/>
 * DependencyInfoProvider allow the library to seek much more info about a
 * dependency of your application than the one provided by an AppInfoProvider.
 * Also those information are only displayed when the user is currently on the
 * page of the dependency. So those InfoProvider are only called and used when the user
 * navigate to this page.
 *
 * @since 0.1
 */
public interface DependencyInfoProvider{
    //TODO
}
